/**
 * Admin DEMO data only. Used by AdminDemoProvider for /admin/demo.
 * Never call backend API here. When you add API for real admin, do NOT change this file.
 */
export {
  mockCompanyRequests,
  mockProducts,
  mockNewsSources,
  mockPropertyListings,
  mockUsers,
} from "./adminMockData"

export { mockAdSubmissions } from "@/contexts/user-context"
